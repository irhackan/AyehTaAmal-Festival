name: Build Android APK

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout project
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Build Debug APK
        working-directory: Source/AyehTaAmal
        run: |
          java -classpath gradle/wrapper/gradle-wrapper.jar \
          org.gradle.wrapper.GradleWrapperMain \
          assembleDebug --no-daemon --stacktrace

      - name: Build Release APK
        working-directory: Source/AyehTaAmal
        run: |
          java -classpath gradle/wrapper/gradle-wrapper.jar \
          org.gradle.wrapper.GradleWrapperMain \
          assembleRelease --no-daemon --stacktrace

      - name: Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: AyehTaAmal-debug
          path: Source/AyehTaAmal/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error

      - name: Upload Release APK
        uses: actions/upload-artifact@v4
        with:
          name: AyehTaAmal-release
          path: Source/AyehTaAmal/app/build/outputs/apk/release/*.apk
          if-no-files-found: error
